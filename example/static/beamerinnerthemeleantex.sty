\mode<presentation>

% Frame title
\defbeamertemplate*{title page}{leantex}[1][]{
  \begin{tikzpicture}[overlay, remember picture]
    \useasboundingbox (0,0) rectangle(\the\paperwidth,\the\paperheight);

    \fill
    [postaction={pattern={Lines[angle=45,distance={40pt},line width=20pt]},
                 pattern color=primary2!10},
     postaction={pattern={Lines[angle=45,distance={60pt},line width=20pt]},
                 pattern color=primary3!10},
    color=primary1!10, line width=2mm]
    ($(-1,-10)$) rectangle ($(-1,-10) + (\the\paperwidth,6)$);





    \path[clip]
    decorate[decoration={coil, amplitude=1mm,segment length=15mm}]{($(-2,-5.25)$) to ++ (\the\paperwidth+80,0)} --++ (0,7.25) --++ ($(-\the\paperwidth-80,0)$) -- cycle;

    \fill[color=primary1, draw=primary1, line width=2mm
    % decorate,decoration={coil, amplitude=2mm,segment length=15mm}
    % ,% ,
    % postaction={pattern={Lines[angle=45,distance={40pt},line width=20pt]},
    %              pattern color=primary2!100!primary3},
    % postaction={pattern={Lines[angle=45,distance={80pt},xshift=30pt,line width=30pt]},
    %              pattern color=primary3!50!primary3}
    ]
    decorate[decoration={coil, amplitude=1mm,segment length=15mm}]{($(-2,-5.25)$) to ++ (\the\paperwidth+80,0)} --++ (0,7.25) --++ ($(-\the\paperwidth-80,0)$) -- cycle;


    % ($(-1,-4)$) rectangle ($(-1,-4) + (\the\paperwidth,6)$);

    % \draw[fill=white!10, rounded corners, radius=10, draw=none]
    % ($(2.75,-5.5)$) rectangle (8.25, -7.25);

    \draw[fill=primary1, rounded corners, radius=10,draw=none]
    ($(0.125,-4.9)$) rectangle (8.1, -3.025);



    % \fill[draw=primary1, fill=none, color=primary1, line width=2mm]
    % ($(0,0) + (0.5, 0.5)$) rectangle ($(\the\paperwidth,\the\paperheight)  - (0.5, 0.5)$);


  \end{tikzpicture}
   \vskip3cm%
    \begin{beamercolorbox}[wd=12cm,leftskip=0cm,sep=8pt,#1]{title page header}
      \usebeamerfont{title}\inserttitle\par%
    \end{beamercolorbox}%
    \vskip0.75cm% 
    \begin{beamercolorbox}[wd=12cm,leftskip=3cm,#1]{author}
      \usebeamerfont{author}\insertauthor%
    \end{beamercolorbox}
     \vskip0.2cm%
    \begin{beamercolorbox}[wd=12cm,leftskip=3cm,#1]{date}
      \usebeamerfont{author}\insertinstitute%
    \end{beamercolorbox}
     \vskip0.2cm%
    \begin{beamercolorbox}[wd=12cm,leftskip=3cm,#1]{date}
      \usebeamerfont{author}\insertdate%
    \end{beamercolorbox}
  \vfill
}

\defbeamertemplate*{itemize item}{leantex}{\mbox{\small
  \raisebox{-.35\height}{
  \begin{tikzpicture}
    \draw[color=primary1, line width=0.8mm] (0.25,0.07125)
    circle (10pt);
    \filldraw[draw=none, fill=primary1, line width=0.8mm]
    (0.25,0.07125)
    circle (5pt);
  \end{tikzpicture}
}
}}

\defbeamertemplate*{enumerate item}{leantex}{{\small
  \raisebox{-.28\height}{
  \begin{tikzpicture}
    % \draw[color=primary1, line width=0.8mm] (0,0) circle (12pt);
    \draw[color=primary1, line width=0.8mm] (0,0.0) circle (10pt);

    \node[color=secondary1-text, text width=10mm,  align=center] at (0,0) {\Large\textbf\insertenumlabel};
    % \draw[color=primary1, line width=1.0mm] ($(0,0) + (14pt,0pt)$) --  ($(0,0) + (20pt,0pt)$);
  \end{tikzpicture}
  }\hspace{-0.5em}
}}


\mode<all>
